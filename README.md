# NovaAI demo

This is a Vite + React + TypeScript + Tailwind project that reproduces the NovaAI landing page behavior described in the prompt.

Run:

- npm install
- npm run dev

Notes:
- The hero uses a CloudFront video URL. For offline dev you may add /hero.mp4 and /hero-poster.jpg to the public folder.
